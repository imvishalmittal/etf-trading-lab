name: RETIRED - Paper ETF floor check
on: workflow_dispatch
permissions: { contents: read }
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RESEARCH_REJECTED — ETF paper floor checks are permanently disabled."
